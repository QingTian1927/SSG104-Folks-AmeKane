---
import type { languages } from "../../i18n/ui";
import { useTranslations } from "../../i18n/utils";

interface Props {
    lang?: keyof typeof languages;
}

const { lang = "vi" } = Astro.props;
const translate = useTranslations(lang);
---

<div class="w-full p-8 flex flex-col items-center justify-center">
    <header
        class="w-full lg:w-3/4 xl:w-3/5 min-h-[40svh] flex flex-col items-center justify-center gap-8 text-center"
    >
        <h1
            class="font-bold text-4xl sm:text-6xl lg:text-7xl tracking-wider text-pretty text-zinc-900 dark:text-zinc-50"
        >
            <span class="inline-block mb-2">Vững tài chính,</span>
            <span class="inline-block">Chắc tương lai!</span>
        </h1>
        <p
            class="font-medium text-base lg:text-lg tracking-tight text-pretty text-zinc-700 dark:text-zinc-200"
        >
            Quản lý tài chính của bạn một cách dễ dàng và hiệu quả. Chúng tôi
            cung cấp các công cụ và tài nguyên cần thiết để bạn kiểm soát chi
            tiêu và tiết kiệm hiệu quả.
        </p>
    </header>

    <div
        class="w-full lg:w-3/4 xl:w-3/5 flex items-center justify-center gap-4 text-zinc-800 dark:text-zinc-100"
    >
        <a
            href="/signin"
            class="p-3 rounded-md font-semibold bg-cyan-500 dark:bg-blue-700 hover:bg-cyan-600 hover:text-zinc-100 dark:hover:bg-blue-900"
        >
            {translate("navbar.signin")}
        </a>
        <a
            href="/about"
            class="p-3 rounded-md font-semibold hover:text-cyan-600 dark:hover:text-blue-700"
        >
            {translate("navbar.about")}
        </a>
    </div>

    <div class="w-full mt-12 p-0 pt-8 lg:p-8">
        <img
            src="/home/hero.jpg"
            alt={translate("home.hero.image.alt")}
            class="w-full aspect-video rounded-xl border-2 border-zinc-300 dark:border-zinc-700 shadow-lg shadow-zinc-300 dark:shadow-zinc-700"
        />
    </div>
</div>
